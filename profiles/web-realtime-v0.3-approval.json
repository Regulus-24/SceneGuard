{
  "profile_id": "web-realtime",
  "version": "0.3-approval-demo",
  "description": "Demo profile requiring human approval before the tested triangle repair. Values are public SceneGuard demo defaults.",
  "rules": {
    "max_file_bytes": 1048576,
    "max_triangles": 1,
    "max_texture_dimension": 1024,
    "allow_external_uris": false,
    "require_embedded_buffers": true
  },
  "repair_policy": {
    "allowed_operations": ["remove_degenerate_triangles"],
    "operation_risk_levels": {
      "remove_degenerate_triangles": "L2"
    },
    "auto_execute_risk_levels": ["L0", "L1"],
    "approval_required_risk_levels": ["L2"],
    "denied_risk_levels": ["L3"],
    "note": "L2 approval demo; the approved plan remains constrained to the same tested deterministic repair."
  }
}
